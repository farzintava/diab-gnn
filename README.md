# Diabetes GNN Project

- Apple Silicon setup with Conda, PyTorch (MPS), and PyTorch Geometric.
- See `configs/` for hyperparams and `Makefile` for common tasks.

## Quickstart
1) `conda activate diab-gnn`
2) Put raw CSV in `data/raw.csv`
3) `make preprocess`
4) `make baseline`
5) `make gnnA`
